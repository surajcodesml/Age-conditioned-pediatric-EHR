"""PIC fine-tuning package.

Builds cohorts and fine-tunes a TALE-EHR pretrained backbone on PIC disease
tasks, reusing the parent ``finetune/`` dataset, tensorizer, trainer helpers,
and model machinery without modifying them.
"""
